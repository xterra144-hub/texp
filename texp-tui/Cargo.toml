[package]
name = "texp-tui"
version = "0.1.0"
edition = "2024"

[dependencies]
texp-core = { path = "../texp-core" }
ratatui = "0.30.1"
crossterm = "0.29.0"
pulldown-cmark = "0.13.4"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
dirs = "6.0.0"
